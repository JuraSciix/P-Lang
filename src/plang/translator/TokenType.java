package plang.translator;

public enum TokenType {

    IDENTIFIER,
    INTEGER,

    LPAREN,
    RPAREN,
    LBRACE,
    RBRACE,
    BANG,
    ASG,
    PLUS,
    MINUS,
    STAR,
    SLASH,
    PERCENT,
    EQ,
    NOT_EQ,
    LT,
    LT_EQ,
    GT,
    GT_EQ,
    AMP,
    AMPAMP,
    BAR,
    BARBAR,

    IF,
    WHILE,
    ELSE,
    RETURN,

    SEP
}
