package plang.translator;

public enum TokenType {

    IDENTIFIER,
    INTEGER,

    LPAREN,
    RPAREN,
    LBRACE,
    RBRACE,
    PLUS,
    BANG,
    MINUS,
    STAR,
    SLASH,
    PERCENT,
    EQ,
    ASG,

    IF,
    ELSE,
    RETURN,

    SEP
}
